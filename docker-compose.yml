version: '3.7'

services:
  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - ./weather-app-main:/usr/share/nginx/html
    depends_on:
      - weather-app

  weather-app:
    build: . 
    ports:
      - "3000:3000"